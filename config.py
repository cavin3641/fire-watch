# -*- coding: utf-8 -*-
"""
설정 파일 - 여기만 고치면 됩니다.
키는 절대 이 파일에 직접 적지 마세요. 환경변수(.env)로 넣습니다.
"""
import os

# ── API 키 (환경변수에서 읽어옴) ────────────────────────────
SAFETY_DATA_KEY = os.getenv("SAFETY_DATA_KEY", "")   # 재난안전데이터공유플랫폼
NAVER_CLIENT_ID = os.getenv("NAVER_CLIENT_ID", "")   # 네이버 개발자센터
NAVER_CLIENT_SECRET = os.getenv("NAVER_CLIENT_SECRET", "")
KAKAO_REST_KEY = os.getenv("KAKAO_REST_KEY", "")     # 카카오 (주소→좌표 변환용)
TELEGRAM_TOKEN = os.getenv("TELEGRAM_TOKEN", "")     # 텔레그램 봇
TELEGRAM_CHAT_ID = os.getenv("TELEGRAM_CHAT_ID", "")

# ── 검색 설정 ──────────────────────────────────────────────
SEARCH_KEYWORDS = [
    # 일반
    "화재", "공장 화재", "상가 화재", "건물 화재", "주택 화재",
    # 수도권 지역별 (지역명을 넣으면 훨씬 많이 잡힙니다)
    "서울 화재", "인천 화재", "경기 화재",
    "고양 화재", "파주 화재", "김포 화재", "부천 화재",
    "남양주 화재", "의정부 화재", "양주 화재", "포천 화재", "구리 화재", "하남 화재",
    "수원 화재", "성남 화재", "용인 화재", "안양 화재", "광명 화재", "안산 화재",
    "시흥 화재", "화성 화재", "평택 화재", "오산 화재", "군포 화재", "의왕 화재",
    "이천 화재", "안성 화재", "여주 화재", "양평 화재", "가평 화재", "연천 화재",
    "동두천 화재", "과천 화재", "아파트 화재", "빌라 화재", "창고 화재", "원룸 화재",
]

# 이 단어가 들어있으면 버림 (관심 없는 화재)
EXCLUDE_KEYWORDS = [
    "산불", "차량 화재", "쓰레기", "훈련", "예방", "캠페인",
    "점검", "소방관 순직", "영화", "드라마",
]

# ── 관심 지역 : 수도권 ─────────────────────────────────────
# 이 시도에서 난 화재만 지도에 올리고 알립니다.
# 나중에 넓히시려면 "강원특별자치도", "충청남도" 등을 추가하세요.
TARGET_SIDO = ["서울특별시", "인천광역시", "경기도"]

BASE_LAT = 37.6584      # 고양시청 - 거리 계산 기준점
BASE_LON = 126.8320

# 수도권 안이라도 이 거리보다 멀면 제외 (0 이면 거리 제한 없음)
MAX_DISTANCE_KM = 0

# ── 기타 ───────────────────────────────────────────────────
OUTPUT_JSON = "docs/fires.json"   # 지도가 읽는 결과 파일
KEEP_HOURS = 48                   # 48시간 지난 건 목록에서 제거
